import Ember from 'ember';

export default Ember.Route.extend({

  model: function(params) {

    // return this.store.findAll('weather-item');

    // return this.store.findMany('weather-item', params.comparables.split(','));

  }

});
